resources:
  - name: MB-GitRepo-6840-0001
    type: GitRepo
    configuration:
      gitProvider: s_gitHub
      path: org_test_automation_MultiBranch
      branches:
        include: .*(([a-z])+.)+[A-Z]([a-z])+$